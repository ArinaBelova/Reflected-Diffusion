defaults:
  - _self_
  - override hydra/launcher: submitit_slurm

# Path to the model
load_dir: "/home/fe/belova/projects/reflected/runs/MNIST/2024.07.09/114138_good" # "/home/fe/belova/projects/reflected/runs/CIFAR10/2024.07.04/170603_good"
w: 0
label: 8 #388

sampling:
  method: pc
  n_steps_each: 1
  noise_removal: True
  snr: 0.01
  predictor: euler_maruyama
  corrector: none
  denoiser: none
  moll: 200
  side_eps: 0.01

denoiser_path: null

eval:
  ckpt: -1
  batch_size: 1 # 100
  rounds: 1024 #1

hydra:
  run:
    dir: vis/${now:%Y.%m.%d}/${now:%H%M%S}
  sweep:
    dir: vis/${now:%Y.%m.%d}/${now:%H%M%S}
    subdir: ${hydra.job.num}
  # launcher:
  #   max_num_timeout: null
  #   timeout_min: null
  #   partition: null
  #   account: null
  #   mem_gb: null
  #   cpus_per_task: null
  #   gpus_per_node: ${ngpus}
  #   constraint: null
